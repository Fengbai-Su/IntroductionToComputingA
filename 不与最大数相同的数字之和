/***************
描述
输出一个整数数列中不与最大数相同的数字之和
关于输入
输入分为两行： 
第一行为N(N为接下来数字的个数，N<=100)； 
第二行为N个整数，以空格隔开； 
关于输出
输出为N个数中除去最大数其余数字之和。（注意，最大数可能出现多次）
例子输入
3

1 2 3 

例子输出
3
************/
#include <iostream>
#include <cstring>
#include <cmath>
#include <cstdio>
#define MAX 2147483647
using namespace std;
int main(){
    int N=0;
    cin>>N;
    int arr[N];
    bool flag[N];
    for(int i=0;i<N;++i){
        cin>>arr[i];
        flag[i]=false;
    }
    int max=-2147483648;
    for(int i=0;i<N;++i){
        if(arr[i]>max)max=arr[i];
    }
    int sum=0;
    for(int i=0;i<N;++i){
        if(arr[i]==max)
            flag[i]=true;
    }//标记最大数
    for(int i=0;i<N;++i){
        if(flag[i]==0)sum+=arr[i];
    }
    cout<<sum;
    return 0;
}
