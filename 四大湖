/**************
描述
我国有4大淡水湖。 

A说：洞庭湖最大，洪泽湖最小，鄱阳湖第三。 
B说：洪泽湖最大，洞庭湖最小，鄱阳湖第二，太湖第三。 
C说：洪泽湖最小，洞庭湖第三。 
D说：鄱阳湖最大，太湖最小，洪泽湖第二，洞庭湖第三。 

已知这4个湖的大小均不相等，4个人每人仅答对一个， 
请编程按照鄱阳湖、洞庭湖、太湖、洪泽湖的顺序给出他们的大小排名。
关于输入
无。
关于输出
输出为4行，第1行为鄱阳湖的大小名次，从大到小名次分别表示为1、2、3、4；第2、3、4行分别为洞庭湖、太湖、洪泽湖的大小名次。
例子输入
例子输出
3

2

1

4
**********/
#include <iostream>
#include <cstring>
#include <cmath>
#include <cstdio>
#include <iomanip>
#define MAX 2147483647
using namespace std;
int main(){
    int a,b,c,d;//po dong tai hong
    for(a=1;a<=4;++a)
        for(b=1;b<=4;++b)
            for(c=1;c<=4;++c)
                for(d=1;d<=4;++d){//只有一句真，即布尔变量相加等于1
                    if((a!=b) && (a!=c) && (b!=d) && (b!=c) && (c!=d) && (d!=a) && ((b==1)+(d==4)+(a==3)==1) && ((d==1)+(b==4)+(a==2)+(c==3)==1) && ((d==4)+(b==3)==1) && ((a==1)+(c==4)+(d==2)+(b==3)==1))cout<<a<<'\n'<<b<<'\n'<<c<<'\n'<<d<<endl;
                }
    return 0;
}
