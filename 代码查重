/********
描述
    五道口某职业学校开设了一门名为计算概论的课程。这门课程开设最初，老师、同学、助教相亲相爱，十分和谐。直到有一天，一名丧心病狂的助教写了一个代码查重程序，之前的温馨和谐的关系开始崩坏。学生们甚至发生了暴动，这个事件被后世成为T大计算机系灭门惨案。 
    十年过去了，为了研究T大计算机系灭门惨案的始末，P大的老师让同学们实现那名丧心病狂的助教写的伤害了无数无辜同学的查重程序的简化版本。 

    输入两份代码，分别统计两份代码中的if，switch，while，for语句的数量，若两份代码中if和switch的数量之和，与while、for之和分别都相等，则判定这两份代码有相互抄袭的嫌疑。【简直丧心病狂！ 
关于输入
第1行一个整数n，代表第一份代码的行数 
随后从2到n+1行为第一份代码 
第n+2行一个整数m，代表第二份代码的行数 
随后从n+3到n+m+2行为第二份代码
关于输出
一行, 两份代码有相互抄袭的嫌疑, 输出Yes，否则输出No
例子输入
3

int main() {

  printf("Hello World!");

}

4

int main()

{

  printf("Hello World!");

}

例子输出
Yes
提示
    We’ll be using a sophisticated cheat checker that compares handins between all students. Please don’t copy another student’s code.
    ********/
    #include <iostream>
#include <iomanip>
#include <cmath>
#include <cstring>
#include <algorithm>
using namespace std;
int main(){
    int n;
    cin>>n;
    cin.get();
    char str1[1000][1000];
    for(int i=0;i<n;++i){
        cin.getline(str1[i], 1000);
    }
    int m;
    cin>>m;
    char str2[1000][1000];
    for(int i=0;i<m;++i){
        cin.getline(str2[i], 1000);
    }
    int cnt1=0,cnt2=0,cnt3=0,cnt4=0;
    for(int i=0;i<n;++i){
        char *p=NULL;
        if(strstr(str1[i],"if")!=NULL){
            p=strstr(str1[i],"if");
            if(strstr(p+2,"(")!=NULL)cnt1++;
        }
        if(strstr(str1[i],"switch")!=NULL){
            p=strstr(str1[i],"switch");
            if(strstr(p+6,"(")!=NULL)cnt1++;
        }
        if(strstr(str1[i],"while")!=NULL){
            p=strstr(str1[i],"while");
            if(strstr(p+5,"(")!=NULL)cnt2++;
        }
        if(strstr(str1[i],"for")!=NULL){
            p=strstr(str1[i],"for");
            if(strstr(p+3,"(")!=NULL)cnt2++;
        }
    }
    for(int i=0;i<m;++i){
        char *p=NULL;
        if(strstr(str2[i],"if")!=NULL){
            p=strstr(str2[i],"if");
            if(strstr(p+2,"(")!=NULL)cnt3++;
        }
        if(strstr(str2[i],"switch")!=NULL){
            p=strstr(str2[i],"switch");
            if(strstr(p+6,"(")!=NULL)cnt3++;
        }
        if(strstr(str2[i],"while")!=NULL){
            p=strstr(str2[i],"while");
            if(strstr(p+5,"(")!=NULL)cnt4++;
        }
        if(strstr(str2[i],"for")!=NULL){
            p=strstr(str2[i],"for");
            if(strstr(p+3,"(")!=NULL)cnt4++;
        }
    }
    if(cnt1==cnt3 && cnt2==cnt4)cout<<"Yes";
    else cout<<"No";
    return 0;
}
